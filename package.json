{
  "name": "ziraproyect",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "build": "astro build",
    "deploy": "astro build && npx gh-pages -d dist"
  },
  "dependencies": {
    "astro": "^5.3.1",
    "gh-pages": "^6.3.0"
  }
}